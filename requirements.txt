duckdb>=1.1.0
streamlit>=1.35.0
pandas>=2.0.0
rapidfuzz>=3.8.0
python-dotenv>=1.0.0
# Any OpenAI-compatible endpoint (Groq, OpenAI, Together, Ollama, ...).
# The provider is chosen by LLM_BASE_URL -- see llm.py.
openai>=1.40.0
python-dateutil>=2.8.0
pyarrow>=15.0.0

# Agent orchestration. langgraph pulls langchain-core; no LangChain agent
# runtime, model wrapper or chain abstraction is used -- see ARCHITECTURE_V2.md §14.
langgraph>=0.2.0

# SqliteSaver ships as its own distribution, NOT inside langgraph. Without it
# `from langgraph.checkpoint.sqlite import SqliteSaver` fails on a fresh clone
# and conversation persistence breaks at startup.
langgraph-checkpoint-sqlite>=2.0.0

# Optional: renders the compiled graph as ASCII for docs/debugging
# (graph.ascii_diagram). Everything works without it.
grandalf>=0.8
